# Library-Management-System-PostgreSQL
This project demonstrates the implementation of a Library Management System using PostgreSQL. It includes various SQL operations such as CRUD tasks, JOINs, GROUP BY, subqueries, stored procedures, and CTAS to manage library resources effectively.
